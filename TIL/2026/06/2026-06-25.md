# TIL - 2026-06-25 (수)

## 오늘 한 일

| # | 작업 내용 |
|---|----------|
| 1 | 정산 배치 API 중복 생성 문제 분석 — DELETE 범위가 agency_id=2만이었는데 배치가 전체 대리점 처리해서 비 Cresoty 데이터 중복 발생 확인 |
| 2 | DELETE SQL 수정 — agency_id=2 조건 제거, 날짜 범위 전체 삭제로 변경 |
| 3 | 날짜 범위 정산 배치 API 구현 — `GET /apis/sales/settlement/range`, `GET /apis/agency/settlement/range` |
| 4 | 단위테스트 작성 — `MonthlySalSettlementServiceTest`, `AgencySettlementServiceTest` (5개 케이스) |
| 5 | Swagger 어노테이션 적용 — 기존 단건 + 신규 range 엔드포인트 모두 문서화 |
| 6 | 배치 서비스 로그 추가 (`@Slf4j`) — 시작/진행 N/총일/완료/오류 단계별 로그 |
| 7 | 백오피스용 대리점 지정 API 구현 — `GET /apis/agency/settlement/range/{agencyId}` |
| 8 | `AgencySettlementServiceTest`에 `executeRangeSettlementByAgency` 테스트 4개 케이스 추가 |

## 배운 것

| # | 내용 |
|---|------|
| 1 | Spring MVC 경로 우선순위 — `/settlement/range` (리터럴) 이 `/settlement/{date}` (PathVariable) 보다 우선 매칭됨 |
| 2 | 정산 배치 쿼리는 agency 필터 없이 전체 대리점 처리 → DELETE 시에도 전체를 삭제해야 중복 방지 가능 |
| 3 | `DateTimeFormatter.BASIC_ISO_DATE` = `yyyyMMdd` 파싱, `ISO_LOCAL_DATE` = `YYYY-MM-DD` 포맷 |
| 4 | `while (!current.isAfter(end))` 패턴으로 날짜 범위 루프 구현 (start == end이면 1회 실행) |
| 5 | 백오피스용 API는 기존 배치 API 수정이 아닌 신규 API 추가로 설계 — 기존 전체 배치 유지 + agency별 신규 배치 분리 |
| 6 | `@Slf4j` Lombok 어노테이션으로 로거 자동 주입 — `log.info/error` 바로 사용, `@RequiredArgsConstructor`와 함께 클래스에 선언 |
| 7 | QueryDSL WHERE 조건에 agencyId 필터 추가로 쿼리 범위 한정 — `.and(a.id.eq(agencyId))`, 기존 메서드 복사 + 조건 1개 추가로 재사용 |

## 이슈 / 트러블슈팅

| # | 이슈 | 해결 |
|---|------|------|
| 1 | Controller에 `@Operation` 어노테이션 중복 — 기존 annotation을 제거하지 않고 새로 추가해서 두 개가 붙은 상태 | 기존 `@Operation` 삭제 후 신규 annotation 단건으로 정리 |
| 2 | `modules:keycloak:compileJava` 오류로 `compileTestJava` 실패 | 기존 pre-existing 오류로 이번 변경과 무관 확인, 테스트 코드 import/로직은 정상 |

## 내일 할 일

| # | 계획 |
|---|------|
| 1 | 실제 DB 정산 재생성 실행 — monthly_sal + agncy_sttl 전체 삭제 후 range API로 2026-02-01 ~ 2026-06-23 재생성 |
| 2 | ~~백오피스용 agency별 재정산 API 설계~~ → 완료 (`GET /apis/agency/settlement/range/{agencyId}`) |
