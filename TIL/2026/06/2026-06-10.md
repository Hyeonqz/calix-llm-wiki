# TIL - 2026-06-10 (수)

## 오늘 한 일

| # | 작업 내용 |
|---|----------|
| 1 | MCC 코드 가맹점 상세 연동 — 아키텍처 설계 및 `docs/03_Features/mcc-code-merchant-detail.md` 기능 문서 작성 (구현은 추후 진행) |
| 2 | `AppMenu.tsx` — 개발자전용 메뉴를 가맹점 관리처럼 2-depth 토글 구조로 변경, 레이블 `개발자` → `개발자전용` |
| 3 | 크레소티 매입반송 배치 — `filterSameDayCancels()` 추가로 당일 승인+취소(순합 0) 거래를 정산 파일에서 제외 (카드: PAY+CANCEL 쌍, 계좌결제: CANCEL-only 원거래 조회) |
| 4 | 크레소티 매입반송 배치 — REFUND_NORMAL/RETURN/HOLD/CLEAR switch 케이스 추가 및 `SettlementAccumulator` 취소매입 전용 필드 4종 확장, `DataTotal.of()` 일반매입+취소매입 합산 처리 |

## 배운 것

| # | 내용 |
|---|------|
| 1 | 코드성 정적 데이터(700개 MCC)는 Redis 캐시보다 컴포넌트 마운트 시 API 1회 호출 + React state 보관이 더 적합 — 기존 `getCities()` / `getTimezones()` 패턴과 동일하게 `Promise.all()`에 추가하는 방식 |
| 2 | AppMenu의 토글 구조는 `items` 안에 `items`를 중첩하면 자동으로 collapsible 처리됨. 레이블 변경 시 `hiddenMenus` 배열과 `finalModel` filter 두 곳 모두 함께 수정해야 함 |
| 3 | `SettlementBatchHistory.transactionNo`는 PAY/CANCEL 행 모두 원거래 PAY의 거래번호를 저장 — 이를 키로 쌍을 매핑하면 in-memory에서 당일취소 판별 가능 |
| 4 | Java에서 enum.equals(String)은 항상 false — `PaymentType.PAY.equals("PAY")`는 절대 true가 되지 않으므로 String 비교 시 반드시 `PAY.name().equals(str)` 사용 |

## 이슈 / 트러블슈팅

| # | 이슈 | 해결 |
|---|------|------|
| 1 | 개발자 메뉴 레이블 변경 시 `hiddenMenus`와 `finalModel` 필터가 구 레이블로 남아있으면 대리점 역할에서 메뉴가 노출될 수 있음 | 세 곳(model, hiddenMenus, finalModel filter) 모두 동시에 수정 |
| 2 | 계좌결제 당일취소는 `SettlementBatchHistory`에 CANCEL 행만 존재 — PAY 행 없이는 in-memory 비교 불가 | CANCEL-only transactionNo를 모아 `Payment` 테이블 IN 쿼리로 원거래 일괄 조회 후 날짜 비교 |
| 3 | `PURCHASE_CLEAR` case에서 `acc.holdClearAmount = acc.holdAmount.add(...)` 오기입으로 보류해제 금액이 보류 금액에 누적되는 버그 | `acc.holdClearAmount = acc.holdClearAmount.add(...)` 로 수정 |

## 내일 할 일

| # | 계획 |
|---|------|
| 1 | MCC 코드 연동 BE Step 1 구현 — `GET /apis/merchant-management/mcc-codes` API 추가 |
| 2 | MCC 코드 연동 FE Step 2 구현 — `Information.jsx` 드롭다운 교체 |
