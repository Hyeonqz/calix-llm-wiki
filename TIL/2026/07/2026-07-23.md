# TIL - 2026-07-23 (목)

| # | 한 일 | 참고 |
|---|-------|------|
| 1 | 폐쇄망 최신 로직 반영 — api/common/gateway/kafka/batch/socket 모듈 최신화, local kafka broker 1대로 축소 | 8268612..199eb91 |
| 2 | Claude 훅 런타임 산출물(`hooks/data`, `__pycache__`, `.session-changes`) git 추적 제외 — 이미 추적 중인 파일은 gitignore만으론 안 빠져서 `git rm --cached` 병행 | 3fe64f7, cba3f6f |
| 3 | 다중 인스턴스 배포 시 컨슈머 그룹 시맨틱 정리 — `spring.application.name`이 상수면 두 서버가 같은 group.id → 브로드캐스트가 아니라 파티션 분배, 그래서 중복 처리 가드가 없어도 안전 | 대화 |
| 4 | Kafka req/rep의 대기 토픽 결정 원리 — 브로커 마법이 아니라 요청자가 `REPLY_TOPIC`/`CORRELATION_ID` 헤더를 실어 보내고 응답자가 그대로 되쏘는 구조 | KafkaReplyHandler.java |
| 5 | `setSharedReplyTopic(true)`는 기능 스위치가 아니라 로그레벨 스위치 — correlationId 필터링은 항상 동작하고 이 플래그는 미매칭 응답 로그를 ERROR→DEBUG로 낮출 뿐, 같은 group.id의 응답 도난은 못 막음 | 대화 |
| 6 | reply consumer group.id 격리 결함 발견·수정 — instance-id 미정의 + 폴백이 상수 applicationName이라 2대 배포 시 응답 도난→15초 타임아웃. `generateInstanceId()`는 호출부 없는 dead code였고 주석과 코드가 불일치 | 1186c6a |
| 7 | OrderVerifyListener 흐름 분석 문서화 — 시퀀스/분기 mermaid, 실패 11분기 중 원장에 실패를 남기는 경로가 단 1개뿐임을 확인 | docs/02_Design/specs/2026-07-23-kafka-request-reply-and-order-verify-analysis.md |
| 8 | req/rep 리스너는 모든 경로가 ack + 예외를 전부 catch → 컨테이너 ErrorHandler/DLT가 사실상 도달 불가 구조임을 확인 | 6ee3895 |
| 9 | OQMS 토픽명을 java 상수(KafkaTopicProperties)에서 kafka.yml로 이관, SpEL `#{@bean...}` → `${...}` 치환 후 클래스 삭제 | fd3a674 |
| 10 | `@KafkaListener#topics`에 콤마 문자열 프로퍼티를 넣으면 토픽 3개가 아니라 `"a,b,c"` 이름의 토픽 1개가 됨 — 배열 원소로 나열하거나 `#{'${x}'.split(',')}` 필요 | 대화 |
| 11 | KafkaHealthIndicator 버그 수정 — 토픽별 판정 람다가 topic 변수를 안 써서 컨테이너 하나만 살아있으면 전 토픽 ACTIVE로 보고되던 문제. 레지스트리에서 구독 토픽을 직접 읽는 방식으로 교체 | fd3a674 |
| 12 | audit consumer group-id에서 무의미한 profile prefix 제거 (브로커가 환경별로 분리되어 있어 격리에 기여하지 않음) | fe02fad |
| 13 | manual-refund 500(Duplicate entry) 원인 규명 — 중복 거래번호를 Snowflake로 디코딩(epoch 1690981378000, ts/node/seq)해 "과거 데이터 충돌"이 아니라 **이번 요청이 53ms 전 발급한 번호**임을 확정 | 대화 |
| 14 | 취소 원장 이중 INSERT 수정 — HanaRefundStrategy가 컨텍스트의 `refundNewPayment`를 안 쓰고 `initializeRefund`를 재호출해 같은 transaction_no로 payment 2회 INSERT. MoreTaPay 전략은 이미 올바른 방식이라 Hana만 이탈자 | 7c2e20d |
| 15 | 유니크 인덱스 이름이 `settlement_no_UNIQUE`인데 실제 대상 컬럼은 `transaction_no` — 장애 로그 오독 유발. core-lib(원본) 수정 대상이라 apps/core 스냅샷을 고쳐도 빌드 무반영 | Payment.java:44 |
| 16 | `catch (Exception e) { throw new X(e.getMessage()) }` 3중 세탁으로 에러코드 소실 + Hibernate SQL 원문(테이블·전체 컬럼)이 응답에 노출됨. 전역 핸들러에 DB_DUPLICATE_KEY 분류기가 이미 있었는데 catch-all이 도달을 막고 있었음 | 7c2e20d |
| 17 | 예외 전파 원칙 정리 — 에러코드 보유 예외(`QRBankException`)와 `DataAccessException`은 그대로 전파해 전역 핸들러가 분류하게 하고, 나머지만 SERVER_ERROR + cause로 래핑 | AbstractRefundStrategy, ManualRefundDispatcher |

> → Promoted to [Kafka 컨슈머 그룹](/messaging/kafka-consumer-group), [Kafka Request-Reply](/messaging/kafka-request-reply)
