# TIL - 2026-07-22 (수)

| # | 한 일 | 참고 |
|---|-------|------|
| 1 | 이미 취소된 원거래 재취소를 게이트웨이 단에서 거절하는 방어 추가 | 684021a |
| 2 | reply consumer `group.id`의 `instance-id` 설정 원리 정리 — `${prop:}`(빈 기본값) → 미설정 시 JVM마다 랜덤, 공유 reply토픽에서 correlation-id 격리의 전제(같은 group.id면 파티션 분할→응답 도난→15초 타임아웃) | OqmsKafkaReplyConfig.java |
| 3 | 하나은행 취소/망취소 통합테스트 시나리오 도출 — 전제(브랜드·approval_no 적재)→취소(RF)→망취소(AT발신+PL관찰) 의존 순서, Critical 회귀 지점 식별 | 대화 |
| 4 | POS 취소·망취소 통합테스트 HTML 문서 작성 — 인터랙티브 체크리스트(PASS/FAIL/BLOCK·메모·진행률, localStorage 저장) + 시나리오별 POS요청 JSON | docs/08_QA/2026-07-22_POS_취소·망취소_통합테스트_시나리오.html |
| 5 | 위 문서에 실행 명령 자동생성 추가 — POS커맨드→curl, abort결과 noti→kafka-console-producer, API/Kafka 주소를 상단에서 바꾸면 전 명령 실시간 반영 | 동일 문서 |
| 6 | orderVerify 실패 시 Payment 원장 실패반영 부재 진단 — PL폴링이 원장만 보므로 원장=가맹점 실패전달 유일통로. "무조건 F" 오답, 실패 분류(결정적=즉시F / 일시적=P유지 재스캔허용 / 식별불가=관측만) 체계 논의 | 대화 |
| 7 | "단위테스트 위한 private→public"은 안티패턴 — 가시성 변경은 증상, 병은 응집도. 정답은 열기가 아니라 추출(독립개념이면 새 타입의 public), Reflection이 package-private보다 나쁨(런타임에 조용히 깨짐) | 대화 |
