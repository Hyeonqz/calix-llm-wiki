# TIL - 2026-07-24 (금)

| # | 한 일 | 참고 |
|---|-------|------|
| 1 | "진행 중 트랜잭션을 중간에 강제 커밋하고 같은 트랜잭션을 이어간다"는 개념 자체가 불가능함을 정리 — 커밋되면 그 트랜잭션은 끝. 대안은 `REQUIRES_NEW`(outer를 suspend→독립 tx 커밋→resume) | 대화 |
| 2 | "시도 기록 + 실패 시 실패 기록" 요구사항은 독립 커밋이 **두 번** 필요 — 실패 기록을 롤백될 outer 트랜잭션에 쓰면 롤백이 그 기록까지 지움. catch에서 REQUIRES_NEW로 기록 후 원본 예외 재던짐 | REF-003 §4-1 |
| 3 | MySQL/InnoDB `REPEATABLE READ`에서 `REQUIRES_NEW`로 커밋한 행을 같은 outer가 `findById`로 못 봄 — 스냅샷이 outer의 첫 읽기 시점에 확정되기 때문. 엔티티를 경계 너머로 왕복시키지 말고 **id만** 주고받아야 함 | 대화 |
| 4 | 같은 상황에서 detached 엔티티 `merge()`도 위험 — load-then-update의 load가 스냅샷에 걸려 transient로 오판→INSERT 시도→duplicate key. 어제 고친 이중 INSERT(7c2e20d)와 동종 함정 | REF-003 |
| 5 | 반면 UPDATE/`SELECT FOR UPDATE`는 current read라 최신 커밋본을 봄 — "쓰기는 되는데 읽기는 안 되는" 비대칭이 혼란의 원인 | 대화 |
| 6 | 리팩토링 백로그 체계를 2단 퍼널로 신설 (착상 `RB-NNN` → 정식 `REF-NNN` → Progressing → done) + 자체완결 HTML 대시보드(리스트→클릭→상세, file:// 더블클릭 동작) | a12c1e1 |
| 7 | `find -iname "*refactor*"`가 기존 `REF-00N_*.md` 백로그를 못 잡아 병렬 체계를 만들 뻔함 — 파일명이 한글/`REF-` 접두라 키워드 검색 사각지대. 새 문서 만들기 전 **폴더를 직접 볼 것**을 CLAUDE.md 규칙에 명시 | a12c1e1 |
| 8 | 등록 직후 뒤집힌 백로그 항목(RB-004) — "replicas를 프로파일 분리(local=1)" 제안 대신 **RF를 낮추는 건 운영 동작 변경**이라 로컬 인프라를 3대로 맞추는 반대 결정. 결정 근거를 남겨 재등록 방지 | a94c34e |
| 9 | 로컬 Kafka 1대→3대 구성 — `InvalidReplicationFactorException: Replication factor: 3 larger than available brokers: 1`. 앱은 뜨지만 토픽이 안 생겨 컨슈머/프로듀서가 죽는 조용한 실패 | a94c34e |
| 10 | 로컬 MySQL 스키마·시드 추가 — `PaymentService`의 `@PostConstruct`가 UnionPay를 조회하고 없으면 예외를 던져 **컨텍스트 생성 자체가 실패**(부팅 불가). 엔티티에서 역산한 DDL은 인덱스·파티션·미매핑 컬럼이 빠짐 | 80500c2 |
| 11 | "부팅 성공 ≠ 스키마 충분" — mpos-server는 push 테이블 5개가 없어도 기동에 성공하고 푸시 기능을 실제 호출하는 순간 터짐. 기동 시점에 조회하지 않는 테이블은 부팅 검증으로 안 걸러짐 | f1e9a83 |
| 12 | `approval_no` 기록 판정을 `approvalNo != null` 간접 판정에서 `paymentState` 명시 전달로 교체 — 호출부가 이미 상태로 결정한 것을 null 여부로 되읽던 구조 | 448bce0 |
| 13 | `handleDiscardAbort`가 `save()` 없이도 반영되는 근거 3단 확인 — 리스너(별도 빈)→프록시 경유라 `@Transactional` 유효 / 트랜잭션 안에서 조회해 managed / `writeAbort`가 7필드 변경 + `updateTime=Instant.now()`라 무조건 dirty 감지 | 대화 |
| 14 | "save가 안 되는 것 같다"는 착시의 원인 — EXECUTED·FAILED는 INSERT 3건이 눈에 보이는데 DISCARDED는 UPDATE 1건이 전부. 결과물 개수 차이지 더티체킹 고장이 아님 | 대화 |
| 15 | `handleCompleteAbort` 오기 수정 — 망취소 **성공**인데 `systemCancellationFailed()`를 호출하고 있었음. 원본 `AbortServiceImpl` 규칙(성공→`Succeeded`)과 대조해 확정 | PaymentAbortResultService |
| 16 | `is_system_cancelled`는 "취소 성공"이 아니라 **"망취소 프로세스 종결"** 표식 — 재선정 쿼리가 `ne(SUCCESS)`로 거른다. 수정 전엔 `state`/`cancelDate` 조건의 우연한 중첩에만 기대고 있었음 | SystemAbortQueryRepositoryImpl |
| 17 | `DateTimeFormatter.withZone`은 입력 타입에 따라 역할이 갈림 — `LocalDate` 입력이면 **no-op**(시간대는 `LocalDate.now()`가 이미 결정), `Instant` 입력이면 **필수**(없으면 `UnsupportedTemporalTypeException`) | DateTimeFormatterUtil |
| 18 | `ClockConfig` 함정 — 주석은 "전체 시스템 시간을 Asia/Seoul로"인데 `Clock` Bean은 `LocalDate.now()` 무인자 호출에 아무 영향 없음(주입처 2곳뿐). JVM TZ도 미고정이라, `payment.date`가 밀리면 당일취소(CANCEL)가 환불(REFUND)로 분류돼 정산이 달라짐 | ClockConfig |
| 19 | `DateUtil.getDate()` 와 `LocalDateUtil.getLocalDateToday()` 가 한 글자도 다르지 않은 동일 구현 — 중복 | 대화 |
| 20 | gradle 모듈 README 15개 + 루트 정비, 로컬 스키마 재생성 절차를 core-lib `generateSchema` 기준으로 정리 | 851760a, e8de5f8 |
| 21 | `devlog` 스킬에 toss 정산 개편기(payments-legacy) 형식을 "다중 블록 뼈대"로 이식 — 자문자답 훅 → 독립 문제블록 N개(문제→트레이드오프→결정→Before/After→추가이점) → 정량 마치며. 기존 단일 서사와 작업 규모로 택일 | ~/.claude/skills/devlog/SKILL.md |
| 22 | devlog 그림 표준을 mermaid 코드로 고정 — before/after 두 장 대비가 개선을 체감시키는 핵심 장치, 다이어그램 노드 라벨도 공개물이라 민감정보 필터 대상 | 위 SKILL.md |
| 23 | calix-wiki 블로그가 mermaid를 렌더하지 못하는 상태였음을 확인 — react-markdown + remark-gfm + rehype-highlight 조합에 렌더러가 없어 mermaid 펜스가 다이어그램이 아닌 코드블록으로 노출됨 | app/blog/[slug]/page.jsx |
| 24 | 블로그 mermaid 렌더링 구현 — 클라이언트 컴포넌트에서 `html.dark` 감지 + MutationObserver로 테마 토글 시 재렌더, `code`/`pre` 오버라이드로 pre 래퍼 제거, rehypeHighlight에 `ignoreMissing` | app/blog/Mermaid.jsx |
| 25 | module-scheduler 패키지 개편 문서가 리팩토링 대시보드에 안 보인 원인 — ADR-008(`05_ADR`)에만 있고 대시보드는 `03_Features/Refactor`의 RB/REF 배열만 렌더. 두 문서 체계가 분리돼 ADR은 자동 노출되지 않음 | ADR-008 |
| 26 | ADR-008을 REF-004로 정식 승격 — TEMPLATE 8섹션 요청서 작성 + 대시보드 FORMAL 등록 (external 13벤더 flat 나열 → 사업자유형 단일축 van/wallet/pg/franchise, merchant는 벤더 아닌 도메인이라 external 밖 승격) | REF-004 |
| 27 | 공용 라이브러리 jar 에 `@SpringBootApplication` 을 담으면 안 됨 — 소비자가 그 패키지를 `@ComponentScan` 하면 설정 클래스로 주워가고 거기 붙은 `exclude={RedisAutoConfiguration}` 이 **소비자 컨텍스트에 적용**됨. `StringRedisTemplate` 이 사라져 `HmacAuthFilter` 주입 실패로 부팅 불가. 도구 클래스를 test 소스셋으로 이동해 jar 에서 제외 | core-lib 35a9843 |
| 28 | 스키마 생성의 주체는 **엔티티 소유자(core-lib)** 이지 소비자 앱이 아님 — 앱으로 뽑으면 Vault/Kafka/Redis 가 다 떠야 하는데다 그 앱 `@EntityScan` 에 등록된 것만 나옴(mpos 는 자체 5개가 더 붙음) | core-lib 2a01705 |
| 29 | 생성기가 뽑은 DDL 을 실제 MySQL 8 컨테이너에 **적용해 보고** 실패하면 파일을 안 쓰게 함 — "생성은 됐는데 적용하면 깨지는" 산출물을 원천 차단. 보정 규칙이 낡으면 적용 단계에서 잡힘 | core-lib 2a01705 |
| 30 | Hibernate 산출 DDL 은 MySQL 8 이 3곳에서 거부 — `datetime(6) default CURRENT_TIMESTAMP`(정밀도 불일치로 `Invalid default value`), `@ColumnDefault("W")`→`default W`(따옴표 없는 varchar 기본값), `not null` 중복. 보정 전엔 20개 테이블이 조용히 실패하고 FK 가 연쇄로 깨짐 | core-lib 2a01705 |
| 31 | core-lib 에 인코딩 설정이 아예 없어 한국어 Windows(cp949)에서 **한글 문자열 리터럴이 런타임에 깨짐** — `options.encoding='UTF-8'` 명시. 기존 SchemaValidator 메시지도 같은 영향을 받던 상태였음 | core-lib 2a01705 |
| 32 | 로컬 Vault 함정 3종 — dev 루트 토큰 ID 에 `.` 금지(개발망 `hvs.…` 재현 불가), `VaultEndpoint.create()` 는 스킴이 **https 고정**이라 `spring.cloud.vault.scheme` 을 무시(로컬도 `-dev-tls` 필요), transit **마운트 경로 자체가 `kek`** 여야 함(`transit/` 에 붙이면 `no handler for route "kek/decrypt/qr-road"`) | qrgw-docker/docker-compose-vault.yml |
| 33 | core-lib `./gradlew test` 3건 실패는 내 변경 탓이 아닌 선재 결함 — SchemaValidatorTest 가 보정 안 된 원본 DDL 로 `ddl-auto=create` 를 돌려 `Invalid default value` 216건이 나고 그 여파로 검증이 깨짐. 단정하지 않고 실제 DDL 오류를 세어 확인 | - |
